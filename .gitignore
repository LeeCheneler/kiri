node_modules/
.kiri/
dist
coverage/
test-results/
playwright-report/
.playwright-mcp/
*.lcov
*.log
.DS_Store

# Local config (providers, …) used for development against a running LLM
/kiri.yaml

# Local workflow fixtures used when dogfooding kiri against its own repo
/workflows/

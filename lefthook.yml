pre-commit:
  parallel: false
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx,json}"
      run: mise exec -- bun lint
    typecheck:
      glob: "*.{ts,tsx}"
      run: mise exec -- bun typecheck

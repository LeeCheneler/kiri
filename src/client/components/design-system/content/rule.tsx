/**
 * Horizontal rule — a hairline divider marking a break between passages of
 * content. Renders the line only; the space above and below it is the
 * caller's layout concern.
 */
export function Rule() {
  return <hr className="border-t border-rule" />;
}

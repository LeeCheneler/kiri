/**
 * Marketing landing page. A static placeholder for now — the real homepage
 * is composed from the design system in a later change.
 */
export function HomePage() {
  return (
    <main className="mx-auto max-w-2xl px-8 py-24">
      <h1 className="font-display text-4xl text-ink">kiri</h1>
    </main>
  );
}

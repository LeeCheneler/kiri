/**
 * Documentation. An empty shell for now — the multi-page docs content is
 * authored in a later change and rendered through the design system.
 */
export function DocsPage() {
  return (
    <main className="mx-auto max-w-2xl px-8 py-24">
      <h1 className="font-display text-4xl text-ink">Documentation</h1>
    </main>
  );
}

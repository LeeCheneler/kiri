/**
 * The name the first-party investigate tool is offered to the model under.
 * Shared so the server tool definition and the client UI that renders its calls
 * agree on the one string.
 */
export const INVESTIGATE_TOOL_NAME = "investigate";
